#include <iostream>
using namespace std;

int main()
{
    int arr[5] = {10, 20, 30, 40, 50};
    int index;

    cout << "Array elements are: ";
    for(int i = 0; i < 5; i++)
        cout << arr[i] << " ";

    cout << "\nEnter the index to access element: ";
    cin >> index;

    try
    {
        if(index < 0 || index >= 5)
        {
            throw index;  
        }

        cout << "Element at index " << index << " is: " << arr[index] << endl;
    }
    catch(int i)
    {
        cout << "Exception caught: Array index out of bounds!" << endl;
    }

    cout << "Program continues after exception handling." << endl;

    return 0;
}

==output====
valid index:
Array elements are: 10 20 30 40 50
Enter the index to access element: 2
Element at index 2 is: 30
Program continues after exception handling.

Invalid index:
Array elements are: 10 20 30 40 50
Enter the index to access element: 7
Exception caught: Array index out of bounds!
Program continues after exception handling.
